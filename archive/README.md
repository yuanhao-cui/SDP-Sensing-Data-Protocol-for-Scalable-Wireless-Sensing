# Archive

This directory contains archived legacy code that is no longer actively maintained.

## wsdp_old/

The previous-generation implementation of the WSDP library (29 modules). Superseded by the current `src/wsdp/` package (v0.4.0+). Retained for historical reference only.

> **Note:** `wsdp_old/` has one-way imports from `src/wsdp` (18 references). No code in the current project depends on `wsdp_old/`.
